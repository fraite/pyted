        if parent is None:
            if mainloop:
                root.mainloop()
        else:
            if mainloop:
                top_level.grab_set()
                root.wait_window(top_level)